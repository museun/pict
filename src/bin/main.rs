extern crate winit;

extern crate pict;
use pict::*;

//use winit::os::windows::*;

fn main() {
    // let hwnd = window.get_hwnd();
    // eprintln!("hwnd: {:?}", hwnd);

    let mut app = App::new();
    app.run();
}
